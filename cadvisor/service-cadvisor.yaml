apiVersion: v1
kind: Service
metadata:
  labels:
    app: cadvisor
  name: cadvisor
  namespace: cadvisor
spec:
  type: NodePort
  ports:
  - name: cadvisor
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: cadvisor